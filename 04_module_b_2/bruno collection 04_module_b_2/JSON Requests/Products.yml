info:
  name: Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/products.json?query=S"
  params:
    - name: query
      value: S
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
