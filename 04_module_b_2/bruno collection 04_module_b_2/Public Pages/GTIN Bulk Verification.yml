info:
  name: GTIN Bulk Verification
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/verify"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
