info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/register"
  body:
    type: json
    data: |-
      {
        "username": "sahil",
        "password": "sahilpass",
        "email": "s@s.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
